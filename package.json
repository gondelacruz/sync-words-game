{
  "name": "sync-words",
  "version": "1.0.0",
  "private": true,
  "description": "SYNC — a two-player sing-off. Laptop plays the song, phones listen, most lyrics nailed wins.",
  "type": "module",
  "main": "server/index.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --env-file=.env --watch server/index.js",
    "test": "node scripts/simulate.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
